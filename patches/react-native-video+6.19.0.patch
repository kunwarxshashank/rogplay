diff --git a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
index 1234567..abcdefg 100644
--- a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
+++ b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
@@ -281,7 +281,9 @@ public class ReactExoplayerView extends FrameLayout implements
     private void updateProgress() {
         if (player != null) {
             if (exoPlayerView != null && isPlayingAd() && controls) {
                 exoPlayerView.hideController();
             }
-            long bufferedDuration = player.getBufferedPercentage() * player.getDuration() / 100;
+            // Use getBufferedPosition() directly instead of getBufferedPercentage() * getDuration() / 100
+            // to avoid integer overflow crash (Ints.checkedCast) with large buffer positions (e.g. live streams)
+            long bufferedDuration = player.getBufferedPosition();
             long duration = player.getDuration();
             long pos = player.getCurrentPosition();
diff --git a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/DRMManager.kt b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/DRMManager.kt
index 1234568..abcdef0 100644
--- a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/DRMManager.kt
+++ b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/DRMManager.kt
@@ -1,13 +1,17 @@
 package com.brentvatne.exoplayer
 
+import android.media.MediaDrm
+import android.util.Log
+import androidx.media3.common.C
 import androidx.media3.common.util.Util
 import androidx.media3.datasource.HttpDataSource
 import androidx.media3.exoplayer.drm.DefaultDrmSessionManager
 import androidx.media3.exoplayer.drm.DrmSessionManager
 import androidx.media3.exoplayer.drm.FrameworkMediaDrm
 import androidx.media3.exoplayer.drm.HttpMediaDrmCallback
 import androidx.media3.exoplayer.drm.LocalMediaDrmCallback
 import androidx.media3.exoplayer.drm.UnsupportedDrmException
 import com.brentvatne.common.api.DRMProps
 import java.util.UUID
 
 class DRMManager(private val dataSourceFactory: HttpDataSource.Factory) : DRMManagerSpec {
     private var hasDrmFailed = false
 
+    companion object {
+        private const val TAG = "DRMManager"
+    }
+
     @Throws(UnsupportedDrmException::class)
     override fun buildDrmSessionManager(uuid: UUID, drmProps: DRMProps): DrmSessionManager? = buildDrmSessionManager(uuid, drmProps, 0)
 
@@ -27,7 +31,12 @@
             }
 
-            val mediaDrm = FrameworkMediaDrm.newInstance(uuid)
-
-            // TODO: This isn't very secure, should be fixed
-            if (hasDrmFailed) {
-                // When DRM fails using L1 we want to switch to L3
-                mediaDrm.setPropertyString("securityLevel", "L3")
+            // For ClearKey: try the requested UUID first, then fallback UUIDs if unsupported
+            val uuidsToTry = if (uuid == C.CLEARKEY_UUID) {
+                listOf(C.CLEARKEY_UUID, C.COMMON_PSSH_UUID)
+            } else {
+                listOf(uuid)
             }
 
-            return DefaultDrmSessionManager.Builder()
-                .setUuidAndExoMediaDrmProvider(uuid) { mediaDrm }
-                .setKeyRequestParameters(null)
-                .setMultiSession(drmProps.multiDrm)
-                .build(drmCallback)
+            var lastException: Exception? = null
+            for (tryUuid in uuidsToTry) {
+                try {
+                    if (!MediaDrm.isCryptoSchemeSupported(tryUuid)) {
+                        Log.w(TAG, "Crypto scheme not supported for UUID: $tryUuid, skipping")
+                        continue
+                    }
+                    val mediaDrm = FrameworkMediaDrm.newInstance(tryUuid)
+
+                    if (hasDrmFailed) {
+                        try {
+                            mediaDrm.setPropertyString("securityLevel", "L3")
+                        } catch (ignored: Exception) {
+                            // ClearKey doesn't support security levels - ignore
+                        }
+                    }
+
+                    Log.d(TAG, "Successfully created DRM session with UUID: $tryUuid")
+                    return DefaultDrmSessionManager.Builder()
+                        .setUuidAndExoMediaDrmProvider(tryUuid) { mediaDrm }
+                        .setKeyRequestParameters(null)
+                        .setMultiSession(drmProps.multiDrm)
+                        .build(drmCallback)
+                } catch (ex: UnsupportedDrmException) {
+                    Log.w(TAG, "DRM UUID $tryUuid unsupported, trying next fallback...", ex)
+                    lastException = ex
+                } catch (ex: Exception) {
+                    Log.w(TAG, "DRM UUID $tryUuid failed, trying next fallback...", ex)
+                    lastException = ex
+                }
+            }
+
+            // All UUIDs failed
+            hasDrmFailed = true
+            if (lastException is UnsupportedDrmException) {
+                throw lastException
+            }
+            throw UnsupportedDrmException(UnsupportedDrmException.REASON_UNSUPPORTED_SCHEME, lastException)
         } catch (ex: UnsupportedDrmException) {
             hasDrmFailed = true
-            throw ex
+            if (retryCount < 1) {
+                return buildDrmSessionManager(uuid, drmProps, retryCount + 1)
+            }
+            throw ex
         } catch (ex: Exception) {
